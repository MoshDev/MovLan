package space.ersan.movlan

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
  @Test
  fun addition_isCorrect() {
    assertEquals(4, 2 + 2)
    //val myValue = Post(id=10, title = "AA", date= Date(), content = "", imageURL = "aaaaa")
    //assertEquals(myValue.id, 10)

//    val json = ExampleUnitTest::class.java.classLoader.getResourceAsStream("post.json")
//            .bufferedReader().use { it.readText() }
//
//    val gsonPost: List<Post> = Gson().fromJson(json, Array<Post>::class.java).toList()
//    println("GSON -> $gsonPost")
//
//
//    println(json)
//    println("hello")
  }
}
